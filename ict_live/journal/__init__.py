"""Trade-record layer — a MINIMAL, data-oriented record for MEASURING the system (not a journal
product). The goal is to answer: does the structural engine work? does execution v1 improve results?
which setups fail? See record.py (schema), generate.py (populate from historical replay + outcomes),
stats.py (expectancy / win-rate aggregation, incl. the v1 TRADE-vs-PASS comparison)."""
